
<core:FragmentDefinition 
    controllerName="urlaubsplaner.urlaubsplaner.controller.Dashboard"
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
    xmlns:unified="sap.ui.unified">
    <PlanningCalendar
            visible="{= ${userDetail>/user/role} === 'Mitarbeiter'  ? true : false }"
            id="EmployeePC"
            rows="{path: 'userDetail>/'}"
            appointmentsVisualization="Filled"
            appointmentSelect="handleAppointmentSelect"
            showEmptyIntervalHeaders="false"
            viewKey="D"
            showWeekNumbers="true">
        <toolbarContent>
            <Title text="Urlaub planen" titleStyle="H4"/>
            <OverflowToolbarButton icon="sap-icon://add" tooltip="Buchen" press="onOpenDialog"/>
        </toolbarContent>
        <views>
            <PlanningCalendarView
                key="D"
                intervalType="Day"  
                description="Wochenansicht"
                intervalsS="1"
                intervalsM="3"
                intervalsL="7"
                showSubIntervals="false">
            </PlanningCalendarView>
            <PlanningCalendarView
                key="M"
                intervalType="Month"  
                
                description="Jahresansicht"
                intervalsS="12"
                intervalsM="12"
                intervalsL="12"
                showSubIntervals="false">
            </PlanningCalendarView>
        </views>
        <rows>
            <PlanningCalendarRow
                icon="{userDetail>pic}"
                title="{userDetail>name}"
                text="{userDetail>role}"
                appointments="{path : 'userDetail>appointments', templateShareable: false}"
                intervalHeaders="{path: 'userDetail>headers', templateShareable: false}">
                <customData>
                    <core:CustomData key="emp-name" value="{userDetail>name}" writeToDom="true"/>
                </customData>
                <appointments>
                    <unified:CalendarAppointment
                        startDate="{userDetail>start}"
                        endDate="{userDetail>end}"
                        icon="{userDetail>pic}"
                        title="{userDetail>title}"
                        text="{userDetail>info}"
                        type="{userDetail>type}"
                        tentative="{userDetail>tentative}">
                    </unified:CalendarAppointment>
                </appointments>
                <intervalHeaders>
                    <unified:CalendarAppointment
                        startDate="{userDetail>start}"
                        endDate="{userDetail>end}"
                        icon="{userDetail>pic}"
                        title="{userDetail>title}"
                        type="{userDetail>type}">
                    </unified:CalendarAppointment>
                </intervalHeaders>
            </PlanningCalendarRow>
        </rows>
    </PlanningCalendar>

</core:FragmentDefinition>